import React from 'react';
import styles from './List.module.scss';

const List: React.FC = () => {
    return (
        <div className={styles.container}>
            <h1>List</h1>
        </div>
    );
};

export default List;
